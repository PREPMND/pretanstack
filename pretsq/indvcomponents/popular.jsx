import { useQuery } from "@tanstack/react-query"
import { usePar}
export default function (){
    const { data , isLoading , error}=useQuery({

    })
    return(
        <>
        <div>
            H
        </div>

        </>
    )
}